import React from "react";
import { useTranslation } from "react-i18next";

const CreateCheckboxConfig = () => {
  const { t: translate } = useTranslation();

  return <span>CreateCheckboxConfig</span>;
};

export default CreateCheckboxConfig;
