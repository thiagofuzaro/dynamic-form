import React from "react";
import { useTranslation } from "react-i18next";

const CreateInputRadioConfig = () => {
  const { t: translate } = useTranslation();

  return <span>CreateInputRadioConfig</span>;
};

export default CreateInputRadioConfig;
